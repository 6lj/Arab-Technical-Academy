<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إنشاء الشهادة - أكاديمية العرب التقنية</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background: linear-gradient(135deg, #0B3D91 0%, #1e40af 100%);
            min-height: 100vh;
        }
        .loading-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
        }
        .loading-spinner {
            border: 4px solid #f3f4f6;
            border-top: 4px solid #0B3D91;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .progress-bar {
            background: #e5e7eb;
            border-radius: 10px;
            overflow: hidden;
            height: 8px;
        }
        .progress-fill {
            background: linear-gradient(90deg, #0B3D91, #1e40af);
            height: 100%;
            border-radius: 10px;
            animation: progress 3s ease-in-out infinite;
        }
        @keyframes progress {
            0% { width: 0%; }
            50% { width: 70%; }
            100% { width: 100%; }
        }
        .pulse {
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
    </style>
</head>
<body>
    <div class="min-h-screen flex items-center justify-center py-8 px-4">
        <div class="max-w-md w-full">
            <!-- Header -->
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold text-white mb-2">إنشاء الشهادة</h1>
                <p class="text-lg text-blue-100">أكاديمية العرب التقنية</p>
            </div>

            <!-- Loading Container -->
            <div class="loading-container p-8 text-center">
                <!-- Spinner -->
                <div class="mb-6">
                    <div class="loading-spinner mx-auto mb-4"></div>
                    <h2 class="text-xl font-bold text-gray-800 mb-2">جاري إنشاء الشهادة...</h2>
                    <p class="text-gray-600">يرجى الانتظار بينما نقوم بإنشاء شهادتك</p>
                </div>

                <!-- Progress Bar -->
                <div class="mb-6">
                    <div class="progress-bar">
                        <div class="progress-fill"></div>
                    </div>
                    <p class="text-sm text-gray-500 mt-2">معالجة الشهادة...</p>
                </div>

                <!-- Steps -->
                <div class="space-y-3 text-right">
                    <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                        <div class="flex items-center">
                            <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center mr-3">
                                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                                </svg>
                            </div>
                            <span class="text-gray-700 font-medium">تحضير البيانات</span>
                        </div>
                    </div>
                    
                    <div class="flex items-center justify-between p-3 bg-blue-50 rounded-lg pulse">
                        <div class="flex items-center">
                            <div class="w-6 h-6 bg-blue-500 rounded-full flex items-center justify-center mr-3">
                                <div class="w-3 h-3 bg-white rounded-full animate-pulse"></div>
                            </div>
                            <span class="text-gray-700 font-medium">إنشاء صورة الشهادة</span>
                        </div>
                    </div>
                    
                    <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                        <div class="flex items-center">
                            <div class="w-6 h-6 bg-gray-300 rounded-full flex items-center justify-center mr-3">
                                <div class="w-3 h-3 bg-gray-500 rounded-full"></div>
                            </div>
                            <span class="text-gray-500">رفع الصورة</span>
                        </div>
                    </div>
                    
                    <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                        <div class="flex items-center">
                            <div class="w-6 h-6 bg-gray-300 rounded-full flex items-center justify-center mr-3">
                                <div class="w-3 h-3 bg-gray-500 rounded-full"></div>
                            </div>
                            <span class="text-gray-500">حفظ الشهادة</span>
                        </div>
                    </div>
                </div>

                <!-- Status Message -->
                <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                    <p class="text-blue-800 text-sm">
                        <strong>ملاحظة:</strong> هذه العملية تتم مرة واحدة فقط. في المرات القادمة ستظهر الشهادة مباشرة.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Auto-redirect to generation endpoint
        setTimeout(() => {
            window.location.href = '/api/generate-certificate-image/<%= certificateId %>';
        }, 2000);
    </script>
</body>
</html>
