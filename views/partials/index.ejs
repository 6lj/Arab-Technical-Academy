<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أكاديمية العرب التقنية - الصفحة الرئيسية</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body { font-family: 'Cairo', sans-serif; }
    </style>
</head>
<body class="bg-gray-50">
    <%- include('partials/header') %>
    
    <!-- Hero Section -->
    <section class="bg-gradient-to-l from-indigo-600 to-purple-600 text-white py-20">
        <div class="container mx-auto px-4 text-center">
            <h1 class="text-5xl font-bold mb-6">تعلم بلا حدود</h1>
            <p class="text-xl mb-8 max-w-2xl mx-auto">انضم إلى آلاف المتعلمين واكتسب مهارات جديدة من خلال دورات احترافية مع أفضل المدربين</p>
            <div class="flex gap-4 justify-center">
                <a href="/courses" class="bg-white text-indigo-600 px-8 py-3 rounded-lg font-bold hover:bg-gray-100 transition">استكشف الدورات</a>
                <% if (!user) { %>
                    <a href="/register" class="bg-transparent border-2 border-white px-8 py-3 rounded-lg font-bold hover:bg-white hover:text-indigo-600 transition">ابدأ مجاناً</a>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="py-12 bg-white">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
                <div>
                    <div class="text-4xl font-bold text-indigo-600"><%= stats.total_courses %></div>
                    <div class="text-gray-600 mt-2">دورة تدريبية</div>
                </div>
                <div>
                    <div class="text-4xl font-bold text-indigo-600"><%= stats.total_students %></div>
                    <div class="text-gray-600 mt-2">طالب مسجل</div>
                </div>
                <div>
                    <div class="text-4xl font-bold text-indigo-600"><%= stats.total_enrollments %></div>
                    <div class="text-gray-600 mt-2">عملية تسجيل</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Courses -->
    <section class="py-16">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">الدورات</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% courses.forEach(course => { %>
                    <div class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition">
                        <img src="<%= course.thumbnail_url %>" alt="<%= course.title %>" class="w-full h-48 object-cover">
                        <div class="p-6">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="bg-indigo-100 text-indigo-600 px-3 py-1 rounded-full text-sm">
                                    <%= convertLevelToArabic(course.level) %>
                                </span>
                                <span class="text-yellow-500">★ <%= course.rating || 0 %></span>
                            </div>
                            <h3 class="text-xl font-bold mb-2"><%= course.title %></h3>
                            <p class="text-gray-600 mb-4 line-clamp-2"><%= course.description %></p>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-500 text-sm">المدرب: <%= course.instructor_name %></span>
                                <span class="text-indigo-600 font-bold"><%= course.students_count %> طالب</span>
                            </div>
                            <a href="/courses/<%= course.id %>" class="block mt-4 bg-indigo-600 text-white text-center py-2 rounded-lg hover:bg-indigo-700 transition">عرض التفاصيل</a>
                        </div>
                    </div>
                <% }) %>
            </div>
            <div class="text-center mt-12">
                <a href="/courses" class="inline-block bg-indigo-600 text-white px-8 py-3 rounded-lg font-bold hover:bg-indigo-700 transition">عرض جميع الدورات</a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-16 bg-gray-100">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">لماذا تختارنا؟</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="bg-white p-8 rounded-lg text-center">
                    <div class="text-5xl mb-4">📚</div>
                    <h3 class="text-xl font-bold mb-3">محتوى عالي الجودة</h3>
                    <p class="text-gray-600">دورات مصممة بعناية من قبل خبراء في المجال</p>
                </div>
                <div class="bg-white p-8 rounded-lg text-center">
                    <div class="text-5xl mb-4">📜</div>
                    <h3 class="text-xl font-bold mb-3">شهادات معتمدة</h3>
                    <p class="text-gray-600">احصل على شهادة إتمام موثقة بعد إنهاء الدورة</p>
                </div>
                <div class="bg-white p-8 rounded-lg text-center">
                    <div class="text-5xl mb-4">⏰</div>
                    <h3 class="text-xl font-bold mb-3">تعلم بمرونة</h3>
                    <p class="text-gray-600">ادرس في أي وقت ومن أي مكان يناسبك</p>
                </div>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>
</body>
</html>